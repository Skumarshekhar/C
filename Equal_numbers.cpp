#include<stdio.h>
int main()
{
	int num1, num2;
	printf("enter the two numbers");
	scanf("%d%d",&num1,&num2);
	if(num1==num2)
	printf("Both numbers are equal");
	else
	printf("Both numbers are not equal");
	return 0;
	}
